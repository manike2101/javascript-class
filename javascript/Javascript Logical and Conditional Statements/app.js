
// AND logical operator
// document.getElementById("demo").innerHTML =
// (true && true) + "<br>" +
// (true && true) + "<br>" +
// (2==3 && 4==4) + "<br>" +
// (2==3 && 3==3);

// OR logical Operator
// document.getElementById("demo").innerHTML =
// (false || false) + "<br>"+
// (true || false) + "<br>" +
// (3==3 || 4==5) + "<br>" +
// (3 < 2 || 2 < 1) + "<br>" +
// (2 > 4 || 1 > 2 );

//NOT Logical Operators
// document.getElementById("demo").innerHTML =
// !(false) + "<br>" +
// !(true) + "<br>" +
// !(1 == 1) + "<br>" +
// !(1 > 2);


/* Assesment
Use AND, OR and NOT operator to compare a boolean comparison,
    a numeric data comparisom for equality, greater than and less than*/

    // answer1 AND
    // document.getElementById("demo").innerHTML =
    // (true && true) + "<br>" +
    // (true && false) + "<br>" +
    // (3 == 5 && 7 == 7) + "<br>"+
    // (5 == 3 && 3 == 3);

    // // answer2 OR
    // document.getElementById("demo").innerHTML =
    // (false || false) + "<br>" +
    // (true || false ) + "<br>" +
    // (7 == 7 || 2 == 5) + "<br>" +
    // (7 < 2 || 3 < 1) + "<br>" +
    // (3 > 7 || 1 > 7);

    // answer NOT
    document.getElementById("demo").innerHTML =
    !(false) + "<br>" +
    !(true) + "<br>" +
    !(7 == 7) + "<br>" +
    !(7 > 1);

    // Read functions and events
